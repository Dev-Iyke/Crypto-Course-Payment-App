{
  "buildCommand": "npx prisma migrate deploy && node --loader ts-node/esm prisma/seed.ts && npm run build",
  "crons": [
    {
      "path": "/api/cron-process-emails",
      "schedule": "0 8 * * *"
    }
  ]
}
